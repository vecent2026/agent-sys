我计划对 `doc/system/BACKEND_DESIGN.md` 进行以下补充，以完善日志清理和数据初始化方案：

1.  **在 `4.5 日志模块` 中增加 `4.5.1 日志清理策略`**：
    *   描述使用 Spring Task (`@Scheduled`) 实现定时任务。
    *   设定 Cron 表达式为 `0 0 2 * * ?` (每天凌晨 2 点)。
    *   执行逻辑：调用 DAO 层删除 `create_time` 早于 30 天前的日志记录。

2.  **新增 `4.6 数据初始化` 章节**：
    *   描述使用 `CommandLineRunner` 接口实现应用启动后的数据检查与初始化。
    *   逻辑：
        *   检查超级管理员账号是否存在。
        *   若不存在，则依次创建：基础权限节点（菜单/按钮）、超级管理员角色、管理员账号（密码加密存储）。
    *   确保系统首次部署启动后即可直接使用。

确认后，我将执行修改。