---
description: 前端 UI 设计与实现规范（基于 ui-ux-pro-max）
alwaysApply: false
---

## 设计总则

- **目标**：统一后台前端的视觉与交互风格，保证可读性、现代感与可维护性。
- **适用范围**：`frontend` 下所有页面，尤其是仪表盘、用户中心、系统管理、登录等。
- **基本原则**：
  - 优先使用 Ant Design 主题 token 与设计系统组件，不在业务代码中随意写死颜色和样式。
  - 所有新增页面、组件都应复用本规范中的配色、排版和组件模式。

## 配色规范

- **基础色板（写入 `design-system/theme.ts`）**：
  - 页面背景：`#F8FAFC`
  - 容器背景：`#FFFFFF`
  - 主色 Primary：`#2563EB`（高饱和科技蓝，主按钮/主高亮/图表主线）
  - 文本主色：`#020617`
  - 文本次级色：`#64748B`
  - 成功：`#16A34A`，警告：`#EAB308`，错误：`#DC2626`
- **约束**：
  - 禁止在页面组件中随意写死新颜色，如需新增色值，先补充到主题或设计系统文档。
  - 图表、进度条等使用统一色板，不为每个页面单独指定随机颜色。

## 字体与排版

- 全局字体（已在主题中配置）：
  - `fontFamily: 'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, 'SF Pro Text', sans-serif`
- 标题层级（参考）：
  - H1 页面主标题：`28–32px`，粗体，少量使用。
  - H2 区块标题：`20–24px`，半粗，用于仪表盘卡片组、。
  - 正文：`14–16px`，常规。
  - 说明文字 / Label：`12px`，次级颜色。
- 行距与间距：
  - 组件之间使用固定间距：4/8/16/24px，对应 `xs/sm/md/lg`，避免随意写「13px/17px」。
  - 副标题统一使用 `Typography.Text` 的次级色，而不是自定义颜色。

## 布局

- **整体布局**：
  - 外层容器与页面可视区域边缘保持 0 间距，整体背景使用页面基础色，无额外包裹大面积白卡。
  - 内部主体区域使用通栏白色背景，圆角为 0，阴影为 0，内容区在可用宽高范围内最大化铺满。
  - 页面位置信息由导航菜单与面包屑统一承担，不单独设置顶部页面主标题区。
  - 页面主内容区域在桌面端优先采用「上方工具栏 + 下方主体内容」的纵向分区结构。

- **布局模式**：
  - 单列布局：
    - 适用于长文案、单条详情、多步骤表单等以纵向阅读为主的场景。
    - 使用 `max-width` 控制行宽在 `1200px` ，页面内容整体水平居中。
  - 左右布局（两栏）：
    - 左侧为功能导航区或列表区，宽度固定在 `280px`，使用粘性定位。
    - 右侧为主内容区，宽度自适应，保持与全局栅格系统对齐。
    - 适用于「左侧目录/列表 + 右侧详情/编辑」的场景。
  - 不分列布局：
    - 列表页、表格页、工作台类页面的主内容区默认采用全宽铺满，不设置 `max-width` 限制。
  - 其他布局
    - 如出现其他需求形式的布局需要先得到允许才能创建页面代码

## 卡片

- **通用卡片**：
  - 背景：`#FFFFFF`
  - 圆角：`15px`（可在主题中统一配置）
  - 内边距：`16px`
  - 阴影：`0 4px 6px rgba(15, 23, 42, 0.08)`（或使用 `boxShadowSecondary`）

## 按钮与控件尺寸

- **统一高度**：
  - 默认控件高度：`32px`（主题中 `Button.controlHeight = 32`）。
  - 小号控件：通过 `size="small"` 得到约 `24px`，仅用于行内操作或次要控件。
- **按钮类型**：
  - 主按钮：`PrimaryButton`（封装 AntD primary），使用主蓝，文字白色，数量要少（每区块 1–2 个）。
  - 危险按钮：`DangerButton`（封装 `danger`），用于删除/高危操作，需要二次确认。
  - 次要操作：描边或文本按钮（`type="default"`/`type="text"`），减少蓝色干扰。
- **交互**：
  - 所有可点击元素必须具备：`cursor: pointer`、清晰 hover 态和 focus 态。

## 渐变与「科技感背景」

- 使用场景限定：
  - 登录页背景、少量 CTA 区域（如营销 Banner），不要覆盖全后台所有页面。
- 推荐风格：
  - 渐变色：蓝 + 紫，如 `#2563EB → #4F46E5 → #7C3AED`。
  - 可叠加低对比度网格线或噪点纹理，但需保证其中文字对比度合格。
  - 重要文字建议放在浅色玻璃卡或白卡上，而非直接叠在高饱和渐变之上。


## 表格页（以用户管理为基准）

- 布局骨架：
  - 页面根容器采用纵向 `flex` 布局，高度占满可视区域（`height: 100%`），背景为白色。
  - 顶部依次为「视图工具区」和「操作工具区」两层：前者用于视图切换、列显示配置等，后者用于新增、导入、导出等主要操作与搜索/筛选入口。
  - 中部为表格区域，放置在独立的滚动容器中，表格开启横向和纵向滚动（`scroll.x = 'max-content'`，`scroll.y` 按视口高度计算），保证表头始终可见且内容区在单屏内完整展示。
  - 底部为固定高度约 `48px` 的底部操作栏，横向两端对齐：左侧为多选批量操作区，右侧为分页区。

- 工具栏与筛选：
  - 顶部操作工具区中，`新增` 操作按钮位于最左侧，其后为 `导入`、`导出` 等其他按钮，再之后为简单搜索与筛选入口，整体保持单行对齐。
  - **创建与编辑**：`创建`、`编辑` 入口点击后均通过右侧抽屉（Drawer）承载表单或详情，不采用弹窗（Modal）或整页跳转。
  - **工具栏按钮宽度**：工具栏中的按钮（新增、导入、导出等）宽度由内容决定，禁止拉伸占满整行；使用 `flex` 布局时，按钮容器需设置 `flex: none` 或 `alignSelf: flex-start`，避免 `flex: 1` 导致按钮过宽。
  - **工具栏区域结构**：表格页布局组件（如 `TablePageLayout`）在渲染 `toolbar` 插槽时，必须用 `alignSelf: 'flex-start'` 或 `width: 'fit-content'` 的容器包裹，避免 toolbar 作为 flex 子元素时被默认 `alignItems: stretch` 拉伸；责任在布局组件，页面传入单个按钮时无需额外包裹。
  - 直接展示的筛选项数量不超过 3 个，全部条件通过「更多筛选」按钮触发的弹层中的组合筛选组件实现。
  - **筛选项即时生效**：筛选项（Select、Input、DatePicker 等）变更后应**立即触发查询**，不额外提供「查询」「筛选」按钮；用户修改任一筛选项即自动发起请求并刷新列表。

- 表格样式与交互：
  - 行高统一为 `40px`，各行之间不额外增加不规则间距，保持紧凑型信息密度。
  - 表格启用 `sticky` 头部，表头固定在可视区域顶部。
  - 列固定规则：第一业务列固定在左侧，操作列固定在右侧，横向滚动时关键字段与操作始终可见。
  - 状态列使用统一的 `StatusTag` 或配置表，而非每页手写颜色，颜色语义与全局状态色保持一致。
  - 启用多选时，行选择列固定在最左侧，并与底部多选批量操作区联动（显示已选数量等信息）。
  - 行内操作按钮使用小号按钮（`size="small"`），危险操作始终使用 `danger` 或红色文本，并通过二次确认（如气泡确认）完成。

- 底部操作栏：
  - 存在多选批量操作时，批量操作入口（如批量打标签、批量移除标签）位于表格左下方，紧跟全选/多选状态展示。
  - 分页组件位于表格右下方，使用小号分页（`size="small"`），显示总条数（`showTotal`）并提供 `20/50/100` 等常用页尺寸切换选项。
  - 批量操作区与分页区处于同一水平操作栏中，始终可见，不随表格纵向滚动而离开视口。

## 抽屉组件

- **底部操作区**：
  - 确认、取消按钮固定在抽屉底部右下角，与抽屉主体内容区有明确分隔（如底部边框或留白）。
  - 按钮顺序：取消在左，确认在右；主操作（确认/保存）使用主按钮样式，取消使用默认或文本按钮样式。

## 实施与演进约定

- 新页面/组件创建前，优先检查：
  - 是否已有可复用的设计系统组件或模式；
  - 是否会引入新的颜色、阴影、圆角，如需新增先更新本规则与 `design-system` 文档。
- 如发现现有页面在颜色对比、可用性或视觉上与本规则冲突：
  - 优先在 `design-system` 中调整主题/基础组件；
  - 避免在单个页面打「局部补丁」破坏一致性。

